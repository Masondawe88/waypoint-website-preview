# V2 CMS MIGRATION · 2026-07-18
Code additions (backwards-compatible, no breaking changes): InventoryItem.pricing object (all fields optional; runtime scaffold fills category-default unit + qualifier + priceOnApplication:true). Sanity: the standalone inventory-item schema file was not present in sanity/schemas (inventory currently lives in code as lib/inventory.ts by design — v1.2 decision, CMS-shaped). When inventory migrates to Sanity, add the pricing object exactly as defined in V2-PRICING-MODEL.md fields plus: bestSuitedTo (text), includedSummary/excludedSummary, minimumTerm, operatingRegion, complementaryAssets (refs), comparisonAttributes. Enquiry documents now receive V2 CRM fields from Fast Lane: enquiryType, assetId, assetName, assetCategory, indicativeRateViewed, enquirySource (+ existing context/seeds/dates/guests/notes/createdAt) — the /api/enquiry Sanity mutate stores the full payload untouched, so no API migration needed. Remain-in-code: brand interface language, engine choreography, Fast Lane mechanics, frozen pages. Move-to-Sanity when connected: inventory + pricing, destination content, journal, imagery via waypointImage (already validated), featured ordering, SEO fields.
