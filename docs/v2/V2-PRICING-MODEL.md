# V2 PRICING MODEL · 2026-07-18
`InventoryItem.pricing`: priceFrom · currency · priceUnit (hour/sector/day/night/week/journey/package/POA) · priceQualifier · minimumTerm · seasonalNote · repositioningNote · includedSummary · excludedSummary · pricingVisibility · priceOnApplication. Single governed renderer `priceDisplay(item)` → "From AUD $8,500 per hour" **only when `priceConfirmed` is true and a figure exists**; otherwise the intentional POA label with a qualifier ("Confirmed to your dates on enquiry · reviewed by a person"). PRICE_BANDS (Under $5k → $50k+) defined; the band filter renders only when priced inventory exists (`hasPricedInventory`). GOVERNANCE DECISION: every current asset ships POA because no rate is commercially confirmed (see content/asset-confirmation.csv — rate structure needed on all 13). This is deliberate, not a gap: acceptance criterion 2 allows intentional POA. To activate a real rate: set `priceConfirmed: true` + `pricing.priceFrom` (code) or the Sanity pricing object once connected — display, cards, index rows, detail pages and Fast Lane rate-carry all light up automatically with "Indicative · subject to dates, routing, season" qualifiers. No live/guaranteed pricing implied anywhere.
