# V2-PRICING-GOVERNANCE · 2026-07-19
Source of truth: content/commercial/pricing-register.ts (22 records) — every public figure traces to a workbook cell (sourceSheet + sourceRow). Governed renderer priceDisplay() remains the ONLY path to a public price; POA items render "Price on application"; no unit conversion anywhere (hourly stays hourly, nightly stays nightly); no combined totals (drawer/engine display per-asset rate basis + "Indicative journey value available once duration, routing and dates are reviewed"). Public disclosure (single reusable line) renders once per fleet/collection page, on detail pages via per-category qualifiers, and in the engine before submission. confirmedBy/confirmedDate/validFrom/reviewDate = null on all records — "Commercial approval metadata pending": workbook confirms figures for build; commercial sign-off metadata to be completed by Mason. Rate changes: edit the register + item pricing together; never edit a rendered string.
