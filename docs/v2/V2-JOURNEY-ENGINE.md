# V2 JOURNEY ENGINE · 2026-07-18
Entry: /begin opens with "Begin with a place, an asset, or simply an idea — a person reviews every journey" (v1.4.1) and accepts seeds from destination/asset/journey deep-links plus session context (ctx=session) from the Ecosystem. Building: Consider-this on any card/detail feeds the Journey drawer — grouped AIR/SEA/STAY/DESTINATIONS/EXPERIENCES, per-item refined summaries (rendered seasonal location line, capacity, rate label, defining line), per-item removal, WaypointJourney.add/remove/seed/open public API, sessionStorage persistence + resume pill. Running summary: the drawer now closes with "INDICATIVE JOURNEY VALUE · available once routing and dates are reviewed · every journey is human-reviewed" — the honest form of a running total while all inventory is POA; when priced items exist, an "Estimated from" sum can replace the line (single insertion point in waypoint-ecosystem.js drawer template). Engine remains 7 full-screen movements with plotted course — deliberately not a form; only email required at submission; posts JourneyEngine lane to /api/enquiry with seeds + considered inventory.
